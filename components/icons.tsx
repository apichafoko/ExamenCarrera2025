import { CircleIcon as CircleNotch, ChromeIcon as Google, CheckCircle, AlertTriangle } from "lucide-react"

export const Icons = {
  spinner: CircleNotch,
  google: Google,
  checkCircle: CheckCircle,
  alertTriangle: AlertTriangle,
}
